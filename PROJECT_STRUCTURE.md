# Структура проекта CSV Analysis Agent

Описание всех файлов и их назначения.

---

## Основные компоненты

### Локальная версия (CLI)

| Файл | Описание |
|------|----------|
| `csv_agent.py` | Основной CLI агент для локального использования |
| `colab_csv_agent.py` | Версия для Google Colab с IPython интеграцией |
| `example_usage.py` | Примеры использования локального агента |

### API Server

| Файл | Описание |
|------|----------|
| `csv_agent_api.py` | API-версия агента с поддержкой JSON и base64 |
| `api_server.py` | FastAPI сервер с REST endpoints |
| `test_api.py` | Скрипт для тестирования API |

---

## Документация

| Файл | Описание |
|------|----------|
| `README.md` | Основная документация проекта |
| `API_DOCUMENTATION.md` | Полная документация API endpoints |
| `API_QUICKSTART.md` | Быстрый старт для API |
| `DEPLOYMENT.md` | Подробные инструкции по деплою на Ubuntu |
| `LOVABLE_PROMPT.md` | Промпт для создания frontend в Lovable |
| `QUICKSTART_RU.md` | Быстрый старт на русском (CLI версия) |
| `PROJECT_STRUCTURE.md` | Этот файл - структура проекта |

---

## Конфигурация и зависимости

| Файл | Описание |
|------|----------|
| `requirements.txt` | Зависимости для CLI версии |
| `requirements_api.txt` | Зависимости для API сервера |
| `.env.example` | Пример файла с переменными окружения |
| `.env` | Ваш файл с API ключом (не в git) |
| `.gitignore` | Файлы, игнорируемые git |

---

## Деплой и настройка сервера

| Файл | Описание |
|------|----------|
| `setup.sh` | Скрипт автоматической установки |
| `csvagent.service` | Systemd service для автозапуска |
| `nginx_csvagent.conf` | Конфигурация Nginx reverse proxy |

---

## Тестовые данные

| Файл | Описание |
|------|----------|
| `example_sales.csv` | Тестовый CSV файл с данными о продажах |

---

## Прочее

| Файл | Описание |
|------|----------|
| `LICENSE` | MIT лицензия |

---

## Структура директорий после установки

```
Claude_code/
├── api_server.py                 # FastAPI сервер
├── csv_agent.py                  # CLI агент
├── csv_agent_api.py              # API версия агента
├── colab_csv_agent.py            # Colab версия
│
├── README.md                     # Основная документация
├── API_DOCUMENTATION.md          # API документация
├── API_QUICKSTART.md             # Быстрый старт API
├── DEPLOYMENT.md                 # Инструкции по деплою
├── LOVABLE_PROMPT.md             # Промпт для Lovable
├── QUICKSTART_RU.md              # Быстрый старт CLI
├── PROJECT_STRUCTURE.md          # Структура проекта
│
├── requirements.txt              # Зависимости CLI
├── requirements_api.txt          # Зависимости API
├── .env.example                  # Пример .env
├── .env                          # API ключи (локально)
├── .gitignore                    # Git ignore
│
├── setup.sh                      # Скрипт установки
├── csvagent.service              # Systemd service
├── nginx_csvagent.conf           # Nginx конфиг
│
├── example_sales.csv             # Тестовые данные
├── example_usage.py              # Примеры использования
├── test_api.py                   # Тесты API
│
├── LICENSE                       # Лицензия
│
├── venv/                         # Виртуальное окружение (локально)
│   ├── bin/
│   ├── lib/
│   └── ...
│
└── outputs/                      # Сгенерированные графики (локально)
    ├── plot_0_1.png
    ├── plot_0_2.png
    └── ...
```

---

## Рабочий процесс

### Для локального использования (CLI)

```
1. Установка
   requirements.txt → venv → .env

2. Запуск
   csv_agent.py → загрузка CSV → чат

3. Результаты
   outputs/ (графики)
```

### Для деплоя API сервера

```
1. Подготовка сервера
   Ubuntu → Python → Nginx → SSL

2. Установка приложения
   requirements_api.txt → venv → .env

3. Настройка службы
   csvagent.service → systemd
   nginx_csvagent.conf → nginx

4. Запуск
   systemctl start csvagent

5. Интеграция с frontend
   LOVABLE_PROMPT.md → Lovable → веб-приложение
```

---

## Workflow разработки

### Локальная разработка

```bash
# Активировать venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements_api.txt

# Запустить в режиме разработки
python api_server.py

# Тестирование
python test_api.py
```

### Обновление на сервере

```bash
# На сервере
cd ~/Claude_code
git pull
source venv/bin/activate
pip install -r requirements_api.txt
sudo systemctl restart csvagent
```

---

## Зависимости между компонентами

```
csv_agent.py (CLI)
└── pandas, numpy, matplotlib, seaborn, openai

csv_agent_api.py (API версия агента)
└── pandas, numpy, matplotlib, seaborn, openai, base64

api_server.py (FastAPI сервер)
├── csv_agent_api.py
├── fastapi, uvicorn
└── python-multipart, pydantic

colab_csv_agent.py (Colab)
└── pandas, numpy, matplotlib, seaborn, openai, IPython
```

---

## Размеры файлов (приблизительно)

| Файл | Размер |
|------|--------|
| csv_agent.py | 16 KB |
| csv_agent_api.py | 14 KB |
| api_server.py | 8 KB |
| colab_csv_agent.py | 17 KB |
| API_DOCUMENTATION.md | 15 KB |
| DEPLOYMENT.md | 20 KB |
| LOVABLE_PROMPT.md | 8 KB |
| test_api.py | 8 KB |
| example_usage.py | 8 KB |

**Общий размер проекта:** ~120 KB (без venv и outputs)

---

## Используемые технологии

### Backend
- **Python 3.8+**
- **FastAPI** - современный web framework
- **Uvicorn** - ASGI сервер
- **Pandas** - обработка данных
- **Matplotlib/Seaborn** - визуализация
- **OpenAI SDK** - для OpenRouter API

### DevOps
- **Systemd** - управление службами
- **Nginx** - reverse proxy
- **Let's Encrypt** - SSL сертификаты
- **UFW** - firewall

### Frontend (через Lovable)
- **React/Next.js**
- **TypeScript**
- **Tailwind CSS**
- **Lovable Cloud Storage**

---

## Порты

| Сервис | Порт | Описание |
|--------|------|----------|
| API Server | 8000 | FastAPI приложение |
| Nginx | 80 | HTTP (redirect to HTTPS) |
| Nginx | 443 | HTTPS |

---

## Переменные окружения

### Обязательные

```bash
OPENROUTER_API_KEY=sk-or-v1-...  # API ключ OpenRouter
```

### Опциональные

```bash
HOST=0.0.0.0              # Хост для API сервера (по умолчанию 0.0.0.0)
PORT=8000                 # Порт для API сервера (по умолчанию 8000)
```

---

## Логи

### Локально
- Вывод в консоль

### На сервере
```
/var/log/csvagent/access.log     # Логи доступа
/var/log/csvagent/error.log      # Логи ошибок
/var/log/nginx/csvagent_access.log   # Nginx access
/var/log/nginx/csvagent_error.log    # Nginx errors
```

Просмотр:
```bash
sudo tail -f /var/log/csvagent/error.log
sudo journalctl -u csvagent -f
```

---

## Полезные команды

### Локально
```bash
python csv_agent.py              # Запуск CLI агента
python api_server.py             # Запуск API сервера
python test_api.py               # Тестирование API
python example_usage.py          # Примеры использования
```

### На сервере
```bash
sudo systemctl status csvagent   # Статус службы
sudo systemctl restart csvagent  # Перезапуск
sudo systemctl logs csvagent     # Логи
sudo nginx -t                    # Проверка конфига Nginx
sudo systemctl reload nginx      # Перезагрузка Nginx
```

---

## Roadmap файлов

Планируется добавить:

- [ ] `docker-compose.yml` - Docker деплой
- [ ] `Dockerfile` - образ контейнера
- [ ] `pytest_tests/` - unit тесты
- [ ] `monitoring/` - скрипты мониторинга
- [ ] `migrations/` - миграции (если добавится БД)

---

## Контрибьюция

При добавлении новых файлов:

1. Обновите этот файл (`PROJECT_STRUCTURE.md`)
2. Обновите `.gitignore` если нужно
3. Обновите `requirements.txt` или `requirements_api.txt`
4. Обновите документацию

---

**Навигация по проекту готова!** 📚
