# Промпт для Lovable AI - CSV Analysis Agent Frontend

Скопируйте этот промпт и вставьте в Lovable для создания frontend приложения.

---

## Промпт для Lovable:

```
Создай web-приложение для AI анализа CSV файлов со следующим функционалом:

## Основные требования:

1. **Главная страница с интерфейсом:**
   - Область для загрузки CSV файла (drag & drop или кнопка выбора)
   - Чат-интерфейс для общения с AI агентом
   - Область вывода результатов (текст, таблицы, графики)
   - История сообщений

2. **Функционал загрузки файла:**
   - Загрузка CSV файла пользователем
   - Сохранение файла в Lovable Cloud Storage
   - Отображение информации о файле (имя, размер, количество строк/колонок)
   - Превью первых строк таблицы

3. **Чат интерфейс:**
   - Поле ввода для вопросов пользователя
   - Кнопка отправки
   - История диалога (вопросы пользователя и ответы AI)
   - Индикатор загрузки во время обработки запроса
   - Сохранение истории в базе данных Lovable

4. **Отображение результатов:**
   - Текстовые ответы AI
   - Сгенерированный Python код (с подсветкой синтаксиса)
   - Графики и диаграммы (из base64 изображений)
   - Таблицы с данными (если есть)
   - Кнопки "Копировать код", "Скачать график"

5. **Backend интеграция:**
   - API endpoint для отправки запросов на внешний сервер
   - URL сервера: https://your-domain.com/api/analyze
   - Метод: POST
   - Данные для отправки:
     * file: CSV файл (multipart/form-data)
     * query: текст запроса пользователя (form field)
     * chat_history: история предыдущих сообщений в JSON формате (form field)

6. **Формат отправки запроса:**
```javascript
const formData = new FormData();
formData.append('file', csvFile); // File object
formData.append('query', userQuestion); // string
formData.append('chat_history', JSON.stringify(chatHistory)); // JSON string

// chatHistory формат:
// [
//   {
//     "query": "Какая средняя цена?",
//     "success": true,
//     "text_output": "Средняя цена: 299.99",
//     "result_data": {...}
//   },
//   ...
// ]

const response = await fetch('https://your-domain.com/api/analyze', {
  method: 'POST',
  body: formData
});

const result = await response.json();
```

7. **Формат ответа от API:**
```javascript
{
  "success": true,
  "query": "Построй график цен",
  "final_code": "import matplotlib.pyplot as plt\n...",
  "text_output": "График построен",
  "result_data": null,
  "plots": [
    "data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..."
  ],
  "attempts_count": 1,
  "timestamp": "2024-01-01T12:00:00"
}
```

8. **UI/UX требования:**
   - Современный, минималистичный дизайн
   - Адаптивная верстка (работает на мобильных)
   - Использовать Tailwind CSS или shadcn/ui
   - Приятная цветовая схема (светлая/темная тема опционально)
   - Анимации при загрузке и появлении результатов
   - Удобная навигация

9. **Дополнительный функционал:**
   - Кнопка "Очистить чат" (начать новый анализ)
   - Кнопка "Загрузить новый файл"
   - Примеры вопросов для пользователя (suggestions):
     * "Покажи описательную статистику"
     * "Построй график распределения"
     * "Найди корреляции между колонками"
     * "Покажи топ-10 записей"
   - Сохранение сессии пользователя (можно вернуться к предыдущим анализам)

10. **База данных Lovable:**
    - Таблица для хранения сессий пользователей
    - Таблица для хранения истории сообщений
    - Таблица для хранения загруженных файлов (ссылки на Cloud Storage)

11. **Обработка ошибок:**
    - Валидация: только CSV файлы
    - Ограничение размера файла (до 50MB)
    - Уведомления об ошибках (toast notifications)
    - Обработка ошибок сети
    - Обработка ошибок от API сервера

12. **Компоненты для создания:**
    - FileUploader component
    - ChatInterface component
    - MessageBubble component (для вопросов и ответов)
    - ResultDisplay component
    - CodeBlock component (с подсветкой синтаксиса)
    - ImageViewer component (для графиков)
    - LoadingSpinner component
    - ErrorNotification component

Технологический стек:
- React или Next.js
- TypeScript
- Tailwind CSS или shadcn/ui
- Lovable Cloud Storage для файлов
- Lovable Database для истории
- Axios или Fetch для HTTP запросов
- react-syntax-highlighter для подсветки кода
- recharts или другая библиотека для preview таблиц

Создай полностью рабочее приложение с красивым UI, готовое к интеграции с моим backend API.
```

---

## Пример использования с Lovable:

### Шаг 1: Откройте Lovable
Перейдите на https://lovable.dev

### Шаг 2: Создайте новый проект
Нажмите "New Project" или "Create App"

### Шаг 3: Вставьте промпт
Скопируйте весь текст выше (начиная с "Создай web-приложение...") и вставьте в Lovable

### Шаг 4: Уточните детали
Lovable может задать вопросы. Вы можете уточнить:
- Предпочтения по дизайну (цвета, стиль)
- Дополнительные функции
- Специфичные требования

### Шаг 5: Настройте URL backend
После создания приложения, замените `https://your-domain.com` на реальный URL вашего сервера

---

## Дополнительные промпты для улучшения:

### После создания базового приложения, можно добавить:

**Промпт 2 - Темная тема:**
```
Добавь переключатель темной/светлой темы в приложение.
Темная тема должна быть приятной для глаз с мягкими цветами.
Сохраняй выбор пользователя в localStorage.
```

**Промпт 3 - История сессий:**
```
Добавь боковую панель с историей предыдущих сессий анализа.
Пользователь должен видеть список всех своих анализов с датой и именем файла.
При клике на сессию - загружается вся история сообщений.
```

**Промпт 4 - Экспорт результатов:**
```
Добавь возможность экспортировать результаты анализа:
- Экспорт всего чата в PDF
- Скачивание всех графиков в ZIP архиве
- Копирование всего кода в буфер обмена
```

**Промпт 5 - Расширенная статистика:**
```
При загрузке CSV файла, автоматически показывай краткую статистику:
- Количество строк и колонок
- Типы данных в каждой колонке
- Процент пропущенных значений
- Первые 5 строк таблицы

Используй endpoint GET /api/schema для получения этой информации.
```

---

## Структура данных для Lovable Database:

### Таблица: sessions
```sql
- id (UUID, primary key)
- user_id (UUID, foreign key)
- file_name (string)
- file_url (string) - ссылка на файл в Cloud Storage
- created_at (timestamp)
- updated_at (timestamp)
```

### Таблица: messages
```sql
- id (UUID, primary key)
- session_id (UUID, foreign key)
- type (enum: 'user_query' | 'ai_response')
- content (text) - текст запроса или ответа
- code (text) - сгенерированный код (если есть)
- plots (json) - массив base64 изображений
- result_data (json) - данные результата
- success (boolean)
- created_at (timestamp)
```

---

## Настройка CORS на backend:

После создания Lovable приложения, обновите CORS в `api_server.py`:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-lovable-app.lovable.app",  # Ваш домен Lovable
        "http://localhost:3000"  # Для разработки
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## Примеры вопросов для демонстрации:

Предложите пользователю эти примеры при первом использовании:

1. "Покажи описательную статистику по всем колонкам"
2. "Построй гистограмму распределения для числовых колонок"
3. "Найди и визуализируй корреляции между данными"
4. "Покажи топ-10 записей по [имя колонки]"
5. "Есть ли пропущенные значения? Покажи статистику"
6. "Построй scatter plot для [колонка1] vs [колонка2]"
7. "Группируй данные по [категория] и покажи средние значения"
8. "Найди выбросы в данных и визуализируй их"

---

## Итоговый чеклист интеграции:

- [ ] Lovable приложение создано
- [ ] Backend API развернут на сервере
- [ ] SSL сертификат настроен (HTTPS)
- [ ] CORS настроен правильно
- [ ] URL backend указан в Lovable приложении
- [ ] Cloud Storage настроен для файлов
- [ ] Database схема создана
- [ ] Тестовый запрос работает
- [ ] Обработка ошибок работает
- [ ] UI/UX протестирован

**Готово к использованию!** 🎉
